import '../../../shared/minigames.js';

const { createMinigameState, nextMinigameState, reduceMinigame } = globalThis.OFICHAOS_MINIGAMES;

export { createMinigameState, nextMinigameState, reduceMinigame };
